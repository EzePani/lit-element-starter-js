import {LitElement, html} from 'lit';

export class FormPage extends LitElement {
  render() {
    return html`
      <h2>Página de formulario</h2>
      <!-- Agrega aquí tu formulario u otro contenido -->
    `;
  }
}

customElements.define('form-page', FormPage);
